
## 🚗 Project: Automobile Showrooms Inventory System

### 🔍 Overview

A desktop-based inventory management application built in **C#**, connected to a **SQL database**, designed to handle showroom operations. 
The system tracks vehicles, customer records, employee data, and accessory inventory across multiple databases in a structured and reliable way.

---

### ✨ Key Features

* **Vehicle Inventory Management**: Add, update, and track vehicle details — including model, make, and attributes.
* **Customer & Employee Module**: Maintain detailed records of customers and showroom staff.
* **Accessory Tracking**: Manage showrooms’ auxiliary items (accessories) via a dedicated module.
* **Persistent Storage**: Uses SQL Server database(s) for robust, multi-database storage and data integrity.
* **Entity-Framework / ORM Integration**: Uses a model-first or code-first approach (via `.edmx` file) to map C# objects to SQL entities.
* **Modular Design**: Clean separation of concerns between domain models (Vehicle, Customer, Employee) and data-access logic (Record, Model Context).

---

### 🛠 Tech Stack

* **Language**: C#
* **Database**: SQL Server (relational database)
* **ORM**: Entity Framework (via `.edmx` model)
* **Application Type**: Desktop / Console (.NET)

---

### 📚 Learning Outcomes

* Developed strong skills in **database-driven application development** using C# and SQL.
* Gained practical experience with **Entity Framework** and database modeling.
* Learned to design modular software with **separation between domain logic and data access**.
* Understood how to maintain data consistency across multiple SQL databases.

---
